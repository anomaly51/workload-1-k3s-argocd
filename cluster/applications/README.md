# Applications

Argo CD `Application` resources for this cluster live here.

Application manifests should point to either `apps/` for application workloads
or `infrastructure/` for platform overlays.
